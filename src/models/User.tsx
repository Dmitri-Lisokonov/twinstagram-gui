export interface User {
    id: string;
    username: string;
    name: string;
    email: string;
    bio: string;
    profilePicture: string;
    token: string;
    followerCount: string;
    followingCount: string;
}